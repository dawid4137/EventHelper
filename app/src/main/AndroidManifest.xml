<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eventhelper">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EventHelper">
        <activity android:name=".GuestListActivity"></activity>
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuSelectActivity" />
        <activity android:name=".InformationActivity" />
        <activity android:name=".WeseleActivity" />
        <activity android:name=".KomuniaActivity" />
        <activity android:name=".SpotkanieActivity" />
        <activity android:name=".UrodzinyActivity" />
        <activity android:name=".ChrzestActivity" />
    </application>

</manifest>